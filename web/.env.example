# Point the dev server at the Python backend.
VITE_BACKEND_ORIGIN=http://localhost:8000

# Supabase auth. Leave both blank to run in keyless dev mode, where you just
# pick a display name and the backend trusts it.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
